<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doafacilapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaPessoaDoadorActivity"></activity>
        <activity android:name=".PessoaDoadorActivity" />
        <activity
            android:name=".TelaInicio"
            android:configChanges="orientation|screenLayout"
            android:label="tela_inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".inicio.EscolhaInicio"
            android:configChanges="orientation|screenLayout"
            android:label="escolha_inicio" />
        <activity
            android:name=".LoginActivityDoador"
            android:configChanges="orientation|screenLayout"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".LoginActivityOng"
            android:configChanges="orientation|screenLayout"
            android:label="login_ong" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat" />
    </application>

</manifest>